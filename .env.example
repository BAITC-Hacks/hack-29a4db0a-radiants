# Development only: forward /api requests to the backend (uncomment when running).
# CAREER_API_TARGET=http://localhost:3000

# Public, build-time API prefix. Never put private keys in VITE_* variables.
VITE_API_BASE_URL=/api
