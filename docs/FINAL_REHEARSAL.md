# Финальная репетиция: Career Quest

## Что уже записано

23 сентября 2026 года записан реальный браузерный сценарий длительностью **2:02**, 1440x900, без озвучки. Это резервная запись **fallback без OpenAI-ключа**, а не доказательство живого AI. Ответы модели не подменялись и не монтировались. Интерфейс прямо показывает недоступность AI и `Rule-based explanation`.

Сборка: `dcc2375`, от `main 56e0929`; приложение идентично проверенному runtime `85a7f26`. Отличия только в документации. Новый Docker-проект `career-quest-recording-20260923`, порт 5197, отдельный новый volume. Начало: 200 / 40 / 60 / 32 / 2743. Конец: 201 сотрудник / 2746 записей истории, каталог не изменён.

Файл `career-quest-fallback-demo.webm` находится локально на ноутбуке AI/Data, передаётся команде отдельно и не добавлен в Git. SHA-256:

```text
82762d6a14f264f3da2125d8a288725657e9bd8dafd34adb511341dc62b69eff
```

Воспроизведение проверено в Chromium: 121.84 секунды, 1440x900, без ошибки декодирования. Просмотрены кадры на 12, 34, 68, 95 и 114 секундах. Запись содержит обновление страницы, но не пересоздание контейнера; последнее проверено отдельно в [отчёте](INDEPENDENT_ACCEPTANCE.md).

## Текст для твоего выступления

Можно говорить своими словами. На резервной записи обязательно сначала назвать её записью и обозначить fallback.

| Время | Что показать и сказать |
| --- | --- |
| 0:00 | «Career Quest связывает развитие сотрудника с конкретным следующим шагом. Это резервная запись без ключа: сейчас видны объяснения по правилам, не ответ AI». |
| 0:03 | «Загружен официальный набор. У сотрудника E0178 цель: Backend Engineer Senior. Readiness 71.3% показывает покрытие требований, а не вероятность повышения». |
| 0:09 | «Первый шаг помогает критическому System Design: уровень 1 станет 2 при требовании 4. Учитываются цель, дефицит навыка и история участия, а не просто самый низкий балл». |
| 0:24 | «Пройденные активности и обязательные мероприятия разделены. Обязательные не выдаются за добровольные рекомендации». |
| 0:31 | «Завершаем активность: System Design вырос, readiness стал 74.1%, появились следующие рекомендации. Уже достигнутый API Design 4 не понижается из-за ограничения курса». |
| 0:39 | «Обновление страницы сохраняет результат. Состояние хранится на сервере, не только в браузере». |
| 0:55 | «Добавляем новый профиль, затем его историю. Для этого не меняем код или каталог. Новый сотрудник получает тот же расчёт и объяснимые следующие шаги». |
| 1:29 | «HR видит общие дефициты навыков, сотрудников без следующего шага и участие в мероприятиях. Можно просмотреть все записи». |
| 1:51 | «Если у Lead нет карьерной цели, система сохраняет его навыки, но не выдумывает следующий грейд. Такой случай требует решения человека». |

Если жюри спрашивает, где AI: «Серверный OpenAI формирует структурированное объяснение по рассчитанным фактам. Код проверяет IDs и ссылки на evidence. При ошибке или превышении времени остаётся воспроизводимый fallback. Живой AI показываем отдельно на ноутбуке Backend». Не называйте ранжирование LLM-ранжированием: в этой версии оно детерминированное.

## Твои оставшиеся действия

1. Один раз проговорить текст с этой записью. Главный акцент: почему выбран шаг, что изменилось после completion и как работает новый профиль.
2. Перед защитой выбрать основной ноутбук и записать его `git rev-parse HEAD`. Для начальных 71.3% нужна чистая отдельная база; очистка localStorage не сбрасывает SQLite. Не удалять рабочий demo volume.
3. Проверить, что резервный WebM открывается на ноутбуке презентации. Передавать только внутри команды: данные синтетические, репозиторий остаётся приватным.
4. Заморозить новые функции. Не обещать доказанный рост вовлечённости, автоматическое повышение, промышленную авторизацию или полную защиту от выдуманных утверждений LLM.

## НАПАРНИК/и

- **Backend:** на основном ноутбуке показать реальный `AI-assisted explanation` для E0178 или E0058; снять короткую запись без ключа, заголовков запросов и содержимого `.env` в кадре. Приложить SHA и ранее полученный безопасный вывод двух live-тестов. Повторять платные тесты только ради ещё одного лога не требуется.
- **Backend:** пройти completion, reload, JSON/CSV import и HR на той же сборке. Инструкция чистого запуска и изоляции базы: [JURY_DEMO.md](JURY_DEMO.md). Перед выступлением убедиться, что выбранная база имеет ожидаемое состояние.
- **Frontend:** проверить читаемость фактического экрана/проектора и доступность HR-таблиц. Новые макеты, анимации и функции сейчас не нужны.

До получения записи/подтверждения с основного ноутбука финальная совместная репетиция с настоящим AI остаётся незакрытой. Локальная fallback-запись её не заменяет.
